The Ring buildpack for Heroku (Forked from The official PHP buildpack for Heroku.)

Contains Apache + Support for Ring (Through CGI)

The package will detect the project by looking at the file : index.ring 
